#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i)
	}
	for (j = 'A'; j <= 'Z'; j++)
	{
		putchar(j)
			return (0)
}
